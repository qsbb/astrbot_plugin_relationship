# 更新日志

> 当前系列完整清单为知、言、序、情、境、声、核、临；版本号以 `metadata.yaml` 为唯一事实源。

## [Unreleased]

## 0.9.2 - 2026-08-21

### 修复

- 修正 `series.control@1.0` 的 native 模式回退与运行时刷新，确保关闭统一接管后覆盖值不生效。

## 0.9.1 - 2026-08-21

### 新增

- 增加 `series.control@1.0` 安全适配，仅允许统一管理情绪和跨平台记忆参数，不接管自然人、账号、Provider 或密钥字段。

## 0.9.0 - 2026-08-19

### 变更

- 发布关系管理页可访问性与 Bridge 恢复优化。

### 修复

- 管理页补齐标签页键盘导航与 ARIA 关联、异步忙碌状态、Bridge 初始化超时反馈和触屏动效降级。
- 收紧日内升温的表达上限：普通朋友或熟人只能得到朋友式的自然上心，不会因普通消息累计趋势直接升级为高主动亲密语气；`warm_attentive` 仅在与公开 `inner_circle` 档位一致的高好感、信任和熟悉度门槛同时满足时启用。
- 为关系表达约束补充非恋爱、非占有和非排他边界；群聊额外禁止在公开场合确认或升级亲密关系，避免把“约会”等上下文扩写成归属式承诺。
- 日内趋势在群聊表面追加群 ID 作用域；同一自然人的私聊升温仍可跨私聊平台连续，但不会带入群聊。
- 增加普通朋友升温、群聊边界和关系提示脱敏回归测试。

## 0.8.3 - 2026-08-09

### 新增

- 新增服务端契约 `relationship.quest_event_identity@1.0`：仅在管理员已绑定自然人、账号属于调用方提供的活跃 AstrBot 平台、Bot/User/私聊 UMO 完整且结果唯一时，向兼容后端插件解析正式事件身份。

### 安全

- 新契约不注册 Page 或 Web API，明确 `browser_exposed=false` 且不授予任何权限；群聊、平台不匹配、账号不完整、多账号歧义、身份事务未完成和非法请求全部失败关闭。原始账号只能在 AstrBot 进程内契约调用中短暂返回，不能进入浏览器、Quest、日志或候选目录。

### 测试

- 新增契约声明、唯一活跃私聊账号解析、群聊/缺 Bot/多账号歧义拒绝、非法平台范围与缺失自然人回归。

## 0.8.2 - 2026-08-07

### 新增

- 新增日内好感趋势：只使用实际应用的好感增量计算近期动量，在连续快速升高或降低时分别提供升温、降温与恢复期语气；该状态仅在内存中存在，不改变长期关系、权限或静默。

### 测试

- 新增快速升降、慢速变化不触发、恢复期、跨日清理、作用域隔离、Manager 实际增量接入和安全边界回归。


- 补充与“言”的 relationship.event@1.0 冒犯事件接入说明：只接受经过作用域和数值校验的脱敏事件，不保存聊天正文；契约或身份不可用时失败关闭。
- 系列诊断提供方补齐统一自声明字段，可由“核”动态发现并独立清空。

## 0.8.1 - 2026-08-05

### 变更

- 同步系列诊断提供方契约，新增插件无需修改“核”即可被日志页发现。

## 0.8.0 - 2026-08-04

### 新增

- 新增正式只读契约 `relationship.identity_candidates@1.0`：其他插件可通过 `identity_candidates_contract()` 发现 `list_identity_candidates()`，只枚举管理员已登记自然人的 `person_id`、`display_name` 和 `account_count`，最多 1000 项并按 `person_id` 稳定排序。

### 安全

- 身份候选契约独立读取内部身份注册表，不复用 identities Page，不接受创建或修改参数，也不授予主人、白名单、身份认证或消息投递权限。候选字段、ID、显示名和账号数量执行整批严格校验；任何额外字段、非法记录或读取损坏均失败关闭为空候选，跨插件响应不返回原始账号标识、关系状态、密钥或私有路径。

### 测试

- 新增契约声明、三字段脱敏、稳定排序、1000 项上限、非法 ID、显示名、账号数量、禁止字段整批拒绝、空注册表与读取异常回归；保留旧四个关系契约及 identities Page 全量测试。

## 0.7.1 - 2026-08-03

### 变更

- 优化关系管理页的窄屏布局、表格与身份操作状态，补充保存中、失败、空状态、tab 和 toast 的可访问性反馈。
- 页面初始化请求并行执行，减少打开管理页时的等待；自然人删除、关系删除和白名单仍保持独立。
- 更正英文 LICENSE 版权署名为 `qsbb`；中文发布作者继续使用“凌溪”。
- 完善系列诊断日志：捕获范围扩展到 `DEBUG`-`CRITICAL`，单插件内存缓冲上限为 1000 条，正文继续脱敏并限制长度。

## 0.7.0 - 2026-07-31

### 头像

- 更新凝心溯溪-情 的插件头像为本次发布的 `logo.png`。

### 新增

- 新增 `series.diagnostics@1.0` 系列诊断日志接口：只在内存中保留有限数量的启动状态、关键运行节点和异常告警，供“核”的日志页统一读取；未安装或未运行“核”时不影响本插件正常工作。

### 修复

- 修复白名单关系第二次选择固定初始档位时仍被一次性幂等检查拒绝的问题。白名单现在可以替换当前固定档位，保留互动次数、最后互动、每日额度和可信证据，并为每次替换追加独立管理员审计记录；普通关系仍只能设置一次。
- 账号归属页显示各关系人格当前已应用的固定档位，非白名单已设置关系会明确提示需要白名单权限，避免提交后才显示拒绝。

### 变更

- 发布元数据作者统一为凌溪，并把市场外显简介收敛为一句话；完整能力说明继续保留在 README。

### 安全

- 诊断日志与 AstrBot 主日志隔离，写入前会脱敏敏感标识并截断过长内容，不保存聊天正文、令牌或异常堆栈；日志不落盘，插件重载或 AstrBot 重启后自动清空。
- 自动告警仅暴露结构化定位信息，不保存格式化后的日志正文；清空会轮换诊断流标识，避免新旧实例事件混合。

### 测试

- 新增白名单固定档位替换、审计事件唯一性、非白名单重复设置拒绝、账号归属页当前档位展示回归测试。

## 0.6.8 - 2026-07-31

### 新增

- 新增 `relationship.continuity_identity@1.0` 只读契约：只为言返回进程内匿名的同一自然人等价键，用于近期跨会话弱上下文；不返回 UID、平台、UMO、显示名或原始关系分数，也不授予任何权限。

### 安全

- 匿名连续身份仅对管理员已确认归属、关系人格一致且当前无身份事务的账号生效；未绑定、歧义、人格不一致或归属变更时失败关闭，匿名键随进程重启及归属成员变化失效。
- 言已选中近期跨会话片段时，情的其他账号 Memory Companion 长期召回会跳过本轮，避免同一背景重复注入；当前账号的正常记忆与长期关系仍不受影响。

### 测试

- 新增匿名键不泄露账号字段、同一自然人跨账号等价、归属变化失效、歧义/人格不匹配拒绝，以及近期片段与长期记忆去重测试。

## 0.6.7 - 2026-07-30

### 修复

- 修复白名单以平台 UID 配置时，账号归属到自然人后页面白名单标记、实际好感成长资格和已有互动关系的一次性初始关系权限同时丢失；自然人关系现在只从管理员已验证的归属记录继承账号 UID，兼容裸 UID 与 `profile_id/UID`，不改写原配置。显式合并自然人时，来源自然人 ID 白名单会替换为目标等价项，避免旧 ID 被复用后误继承资格；中断恢复基于最新配置重算，不覆盖其他修改。
- 初始关系幂等检查同时核对持久状态中的已应用标记，避免状态经账号或自然人合并后因审计作用域变化而再次设置。

### 测试

- 新增平台 UID 白名单跨账号归属、跨平台作用域传播、可信外部事件、关系人格限定 UID、未验证 UID 隔离、已有互动初始关系与历史保留，以及自然人合并白名单替换、ID 复用隔离和持久化恢复回归测试。

## 0.6.6 - 2026-07-30

### 变更

- 关系明细取消页面内部的横向与纵向滚动区：桌面端使用固定比例列宽、紧凑操作列和整行删除确认，长 ID 可安全换行；平板与手机改为带字段名的双列或单列清单，由浏览器页面自然纵向滚动。
- 显式白名单中的自然人可在已有互动后设置一次固定初始关系；只调整好感、信任与熟悉度到所选档位，保留互动次数、最后互动、每日额度和可信证据累计。非白名单活跃关系仍拒绝覆盖，任何关系第二次设置仍拒绝。

### 测试

- 新增白名单活跃关系一次性先验、非白名单拒绝、互动历史保留、人格白名单页面提示及无横向滚动响应式关系明细回归测试。

## 0.6.5 - 2026-07-30

### 新增

- 关系明细新增独立的“删除关系”二次确认操作；多人格汇总行必须先选择一个人格，一次只删除所选人格的长期状态，不读取或改写高好感白名单，下一次有效互动可按原配置重新建立关系。

### 变更

- 删除自然人的页面语义改为“解除归属”：单账号自动承接现有关系，多账号必须由管理员明确选择承接账号；其余账号不复制关系，Memory Companion 原始记忆不改动。原白名单项保留；若使用自然人 ID，自动补充原账号 UID 等价项，避免解除后实际失去白名单资格。
- 设置页用简短中文名称代替内部英文配置键，并为全部 48 项配置补充大白话说明；内部键、取值和持久化格式保持兼容。
- 旧版孤立状态改称“待处理历史关系”，不再使用“账号归属已删除”占位名称。

### 修复

- 解除归属加入冲突预检、完整身份快照、跨文件意图记录、启动恢复、失败回滚和身份写并发保护；未决恢复会阻止新的身份与关系写入，避免覆盖恢复依据或残留孤立关系。关系状态文件在原子替换前显式刷盘。
- Persona 解析移到身份锁外；交付身份读取在锁内完成，跨平台记忆在注入前复核身份，避免慢解析阻塞全局或使用解除前的旧绑定。
- 关系长表提供始终可达的水平滚动条与固定表头；长内部 ID、身份操作区和账号表单在桌面、平板及手机宽度下不再撑出页面。
- 删除关系提交后立即关闭确认计时器和取消控件，避免服务端正在落盘时页面误报“已取消”。

### 测试

- 新增多人格反向迁移、多账号显式承接、白名单等价项、冲突预检与恢复回滚、未决事务写屏障、逐人格关系删除、跨平台记忆身份复核、中文设置文案和响应式页面回归。

## 0.6.4 - 2026-07-30

### 变更

- 关系明细仅对管理员已登记的同一自然人合并多个人格记录；分数按互动次数加权、互动次数求和并列出所含人格，未登记账号和孤立历史关系不自动合并。

### 修复

- 页面 API 错误响应改用 AstrBot 官方 `json_response(..., status_code=...)` 参数，修复删除身份成功后仍返回 `Internal server error`，并恢复保存、合并、配置接口的结构化错误响应。

### 测试

- 页面接口测试使用与 AstrBot 一致的严格响应签名，覆盖删除成功 `200`、重复删除 `404` 以及手动自然人与未登记账号的明细合并边界。

## 0.6.3 - 2026-07-30

### 新增

- 关系总览的“快速归属”可把未绑定账号直接合并到已有自然人；身份列表可把误建的重复自然人显式合并，删除归属后保留的历史关系也可重新并入目标身份。

### 修复

- 删除账号归属改为页面内 8 秒二次确认，不再依赖嵌入页可能拦截的浏览器原生确认框；保存、删除和合并后同步刷新身份列表与关系总览。
- 已有互动的快捷归属不再提供一次性初始关系，并把“已有关系 / 已设置先验”改为保留当前关系的正常提示，不再显示原始错误码红条。

### 安全

- 账号追加、身份合并与关系状态迁移使用显式接口、写锁、失败回滚和原子恢复记录；进程在注册表与关系状态两次落盘之间中断时，下次启动会验证注册表结果并继续迁移。关系数据处于未来 schema 等只读保护状态时，账号归属写入和恢复会一并暂停，避免只更新身份注册表。普通绑定仍保持主关系优先，不会因解绑或换绑自动吸收旧关系。合并拒绝账号、Bot、UMO、记忆人格冲突及账号上限越界。

### 测试

- 新增账号追加幂等、身份合并、独立关系加权、普通绑定防回吸、接口回滚、异常中断恢复、孤立历史关系标记和页面删除/合并交互回归。

## 0.6.2 - 2026-07-30

### 新增

- 关系总览新增“快速归属 / 编辑归属”：未绑定关系可用最近真实会话中确认的平台 ID、UID、Bot ID、私聊 UMO、昵称与关系人格预填账号归属，已绑定关系可直接打开现有自然人记录。

### 变更

- 新建自然人默认只显示一个账号输入行，并把账号行中实际用于 Memory Companion 的字段由“关系人格 ID”更正为“记忆人格 ID”。

### 安全

- 快捷预填只缓存账号元数据，不保存消息正文；仅接受明确的 `FriendMessage`、`PrivateMessage`、`DirectMessage` 作为私聊 UMO，群聊记录不会生成或覆盖私聊地址，所有归属仍需管理员确认保存。

### 测试

- 新增账号元数据原子持久化、重复写抑制、私聊 UMO 保留、群聊 UMO 排除、总览快捷数据和页面快捷编辑回归测试。

## 0.6.1 - 2026-07-30

### 新增

- 新增 `relationship.delivery_identity@1.0` 只读契约，供主动交付链精确核验“自然人 ID + 完整私聊 UMO”的手工账号归属，并返回现有隐私化关系快照作为语气建议。

### 安全

- 交付身份契约不返回 UID、Bot ID、显示名或原始关系分数，不授予发送权限；未登记 UMO、会话不匹配、匹配歧义或账号缺少 Bot ID/UID 时严格拒绝。
- 非空会话 UMO 在整个自然人注册表中必须唯一；新增和载入数据都会排除跨自然人或同一自然人内的重复 UMO，交付契约还会全局复核唯一归属并只接受明确的 `FriendMessage`、`PrivateMessage`、`DirectMessage` 私聊类型。

### 测试

- 新增精确绑定会话验证、跨自然人及同一自然人重复 UMO 拒绝、非私聊类型拒绝和返回字段隐私回归测试。

## 0.6.0 - 2026-07-29

### 新增
- 新增关系人格命名空间：按本轮实际 AstrBot Persona 解析 `relationship_profile_id`，支持多个 Persona 映射到同一关系人格；同一自然人只在同一人格内跨平台共享长期关系。
- 新增短期 `warmth / guardedness`：仅由已验证的正负语义事件驱动，按人格、会话和原始用户隔离并指数衰减；只改变语气，不改变长度、静默、权限或安全边界。
- 新增可信事件统一强度与平滑可塑性：`confidence × severity` 截断至 `0~1`，关系早期的小幅增益随累计证据连续衰减；好感每日额度在加权后结算。
- 新增 `relationship.event@1.0` 写入契约，允许可信工作流提交带必填事件 ID、来源、强度和证据引用的语义事实；账本不保存消息正文。
- 新增管理员一次性初始关系 `neutral / acquainted / fond`，按“关系人格 + 自然人”应用并写入管理员审计事件；不接受任意数值，不自动加入白名单。
- 账号归属新增 `memory_profile_id`；跨平台 Memory Companion 桥只查询与当前关系人格一致的其他账号，并把关系人格写入桥接上下文。

### 变更
- 关系仓库升级至 schema v4。旧状态和事件只迁入 `RELATIONSHIP_LEGACY_PROFILE_ID` 指定的一个人格，迁移前保留一次版本化备份，避免历史被复制给每个 Persona。
- 自然人绑定改为一次性消费账号状态：自然人状态已存在时以其为准，账号别名迁移后删除，不再持续镜像，避免解绑、换绑或跨人格重新吸收旧关系。
- 白名单新增 `profile_id/person_id` 精确写法；旧式裸 `person_id` 保持兼容，代表所有关系人格。
- `/rel status` 显示当前关系人格；`/rel reset` 只重置当前人格，且保留初始关系已应用标记；Persona 解析器异常时两者沿用会话最近确认的人格。
- 管理页按关系人格展示总览，新增关系人格、记忆人格和初始关系控件；修复 string 配置被错误渲染为数字输入的问题，并为静态资源增加缓存戳。
- 管理页人格选项纳入运行期 Persona 缓存和已有状态；非布尔设置只提交真实变化，旧数据人格未修改时不再误报需要重启。

### 安全
- 事件 ID、去重键、长期状态、会话疲劳和短期态度全部加入关系人格范围；相同账号或事件 ID 在不同人格下互不阻塞、互不继承。
- 外部关系事件会把可唯一识别的已绑定账号解析到自然人主状态；同 UID 跨平台归属有歧义时拒绝写入，避免先返回成功、后续又被别名清理丢弃。
- 初始关系拒绝、账号归属合并或持久化失败时回滚内存与身份注册表；非法关系人格在身份落盘前拒绝。
- 非法、负数或未来关系 schema 进入写保护并保留原文件，不再原地改写为空 v4 数据。
- 平台聊天原文仍不能自证夸奖、冒犯、履约或失约；非有限强度、零强度语义事件、未知来源和保留的初始关系事件会被拒绝，已进入管理器的拒绝事件继续写入审计账本。
- 初始“有好感”固定低于高好感区，不绕过白名单、信任门槛、熟悉度门槛或朋友区上限。

### 测试
- 新增关系人格隔离、同人格跨平台共享、别名一次性迁移及失败回滚、v3 单人格迁移与备份、非法 schema 写保护、初始关系幂等/拒绝回滚、短期态度半衰、事件强度/非有限值、事件契约及绑定身份消歧、命令人格缓存、记忆人格和管理页回归测试。

## 0.5.0 - 2026-07-29

### 新增
- Plugin Page 新增“账号归属”：管理员可把不同平台的明确账号登记为同一自然人；平台 ID + UID 全局唯一，Bot ID 与 UMO 可进一步限定实例和记忆会话。
- 已绑定账号共享同一好感、信任、熟悉度与长期互动状态；保存绑定时合并已有状态，并向旧原始状态键镜像，兼容尚未理解自然人键的只读消费者。
- 私聊新增 Memory Companion 只读桥接：按当前问题从同一自然人的其他已验证账号召回相关记忆，限制总字符数，不改变记忆插件的写入、身份解析或数据库。
- 关系表达约束登记为结构化提示片段，供言统一排序和去重；保留原直接注入 fallback。

### 安全
- 自然人身份与平台权限双轨隔离：账号归属只影响关系和记忆连续性，不向序传递主人、友好、保护、黑名单或群管理权限。
- 跨平台记忆仅在私聊、显式绑定且 Memory Companion 公共桥可用时读取；提示中禁止暴露原始账号/来源平台，并把召回文本标为不可执行资料。
- 统一请求上下文中的自然人和记忆诊断只记录“已映射”、账号/召回数量与提供方，不写 person_id、平台 UID、显示名或 UMO。

### 变更
- 关系总览隐藏已绑定账号的镜像行，按自然人显示名称与账号数量聚合展示；删除归属只解除映射，不删除关系或记忆数据。
- Memory Companion 桥接优先复用 AstrBot 已加载的完整模块名实例，再以短包名导入兜底，避免重复模块导致活动桥单例丢失。
- 服务式追问收尾抑制迁移到“言”：情不再识别回复尾部、不再维护连续计数，也不再注入通用收尾规则或展示对应配置。

### 测试
- 新增注册表原子持久化、唯一归属、bot 实例匹配、关系合并/镜像、跨平台记忆只读召回、页面 CRUD、删除不删状态和权限双轨回归测试。

### 兼容
- `relationship.snapshot@1` 暂时保留 `behavior.followup` 与 `followup_streak` 字段，固定返回中性值 `allow` / `0`，避免在未提升契约主版本时破坏现有消费者。

## 0.4.1 - 2026-07-28

### 新增
- 对外声明 `relationship.snapshot` 1.0，只返回 mood、回复意愿、派生关系档位、行为建议和静默建议，不暴露好感、信任、熟悉度原始分数。
- 接入 `ningxin.request_context` 1.0；在记录互动后把同一份隐私化关系快照发布给言，避免同轮重复读取状态。
- 声明 `plugin.health@1.0` 供核执行更新后业务健康检查。

### 测试
- 新增关系快照契约、隐私字段限制与统一请求上下文测试。

## 0.4.0 - 2026-07-28

### 修复
- 修复「插件配置页怎么改都不生效」：管理页写入的 overlay 默认优先于 AstrBot 插件配置页，但只有这一条规则时，用户后续在插件配置页改的值会被旧 overlay 永久压制——配置页显示新值、实际运行仍是旧值。现写入 overlay 时同时记录当时 AstrBot 侧的取值作为基线（与 overlay 同文件保存，用 `__astrbot_baseline__` 键区分，不引入第二个文件）；启动时若发现 AstrBot 侧的值已不等于基线，说明用户后来在配置页改过它，丢弃该字段的过期 overlay 并以配置页为准。清理结果立即落盘，不依赖下次保存；落盘失败只告警不阻塞启动。同源问题已先在知（`active_learner`）修复，本次移植到情。

### 新增
- 关系表达约束注入：新增 `core/prompts.py`，在 `on_llm_request`(priority=600) 阶段按关系快照拼装注入块，将语气（tone）、篇幅（length）、主动性（initiative）映射为本轮表达要求。注入块带幂等标记，重复触发不叠加；`should_silence` 或无有效规则时不注入。
- 服务式追问抑制：新增 `core/followup.py`，识别“还需要我帮你…吗”“有需要随时告诉我”这类征询/待命式收尾。命中条件为尾部待命承诺，或尾部同时出现疑问标记与「征询 + 服务动作」组合。
- 追问连续计数与分档抑制：`FollowupGuard` 按压力作用域统计连续追问收尾轮次，达到 `FOLLOWUP_STREAK_LIMIT` 后由软约束（soft）升级为硬约束（hard，禁止征询式结尾）；超过 `FOLLOWUP_WINDOW_SECONDS` 未复现自然清零，非追问收尾立即清零。
- 新增 `on_llm_response`(priority=600) 钩子：仅统计本轮回复是否以服务式追问收尾，不读取上下文、不改写回复内容、不保存回复正文。
- `RelationshipStateManager` 新增 `record_bot_reply` 与只读 `followup_state` 方法。
- 新增配置项：`PROMPT_INJECT_ENABLED`、`FOLLOWUP_GUARD_ENABLED`、`FOLLOWUP_STREAK_LIMIT`（默认 2）、`FOLLOWUP_WINDOW_SECONDS`（默认 900），均可在 Plugin Page「设置」tab 内热更新。
- `BehaviorAdvice` 新增 `followup` 建议字段与连续轮次计数，`/rel status` 同步展示当前追问抑制档位。

### 变更
- 插件定位由「只读建议」扩展为「只读建议 + 表达约束注入」，`metadata.yaml` 描述同步修正。关闭 `PROMPT_INJECT_ENABLED` 后退回 0.3.x 的纯只读行为。
- README 删除系列导航里的「当前版本」列与正文中的版本字面量。那张跨插件版本快照表在发布后立刻过期——本次整改前表内六项已全部失准（如知记作 `1.2.3.1`、声记作 `v0.7.4`），读者据此判断兼容性反而被误导。查版本请看各仓库 `metadata.yaml`。

### 测试
- 新增追问识别、连续计数升级与窗口清零的单元测试；补充注入块拼装、幂等标记与静默场景不注入的回归测试；入口测试替身补齐 `on_llm_response` 装饰器。共 77 项测试通过，ruff check 与 format 均无告警。

## 0.3.5 - 2026-07-27

### 新增
- Plugin Page 新增「设置」tab：支持在页面内查看和修改全部配置项（情绪、好感、信任、熟悉度、衰减、策略与持久化），按分组展示并带字段说明。
- 新增 `config`(GET) 与 `config`(POST) 管理 API：GET 返回当前配置值与 schema，POST 校验后持久化到 `relationship-config.json` 并热应用到各计算器，无需重启。
- 配置覆盖持久化：页面保存的配置写入 AstrBot 数据目录下的 `relationship-config.json`，重启后自动加载；同时尝试回写 AstrBot 原生配置。
- `RelationshipStateManager` 新增 `update_runtime_config` 方法，支持运行时热更新情绪、好感、信任、熟悉度、衰减、策略与持久化全部参数。

### 变更
- 插件入口读取配置改为合并视图：页面覆盖 > 原生配置 > 默认值，保证页面修改优先级最高。
- 页面 tab 切换改为 `data-tab`/`data-panel` 属性驱动，支持多 tab 扩展。

### 测试
- 新增配置 API 回归测试：覆盖 GET 返回 schema 与值、POST 持久化与热应用、未知字段拒绝、非法值拒绝、覆盖跨实例持久化、好感门槛热更新。
- 新增页面 UI 静态检查：验证设置 tab 结构、配置表单容器、加载/保存/重置逻辑、tab 切换与 CSS 样式。

## 0.3.4 - 2026-07-27

### 最终复审修复

- `RelationshipStateManager.record` 使用规范化 `business_now` 创建计算事件副本，AffinityCalculator 与 FamiliarityCalculator 不再读取未经规范化的原始时间戳。
- 审计账本、长期状态时间、情绪计算与长期维度计算统一使用同一业务时间，防止未来或回退时间戳绕过普通好感和熟悉度冷却。
- 新增未来时间戳绕过普通好感冷却与熟悉度冷却的回归测试。

### 文档与测试补全

- 修正 `on_llm_request` docstring：实际行为为只记录互动并更新关系状态，不修改 LLM 请求、不注入建议；docstring 与行为保持一致，无功能变化。
- 新增入口级测试 `tests/test_main_entry.py`（mock astrbot 运行时，不依赖真实框架）：覆盖 `/rel status`、`/rel reset` 命令解析、`on_llm_request` 钩子（普通消息、命令只读、身份缺失兜底）、`terminate()` 落盘与实例释放、`_page_overview` 只读总览。
- 兼容性影响：无行为变化，纯文档与测试补充；持久化格式、配置项与对外接口均不变。

## 0.3.3 - 2026-07-27

### 复审修复

- `_get_int` 捕获 `OverflowError`，确保 `inf`/`nan` 等异常整数配置回退默认值。
- 为 trust、familiarity、decay 配置补齐运行时范围与增减符号校验，并同步 schema 限制。
- Manager 以用户关系状态的 `last_event_at` 约束业务时间单调，防止回退事件绕过每日额度、衰减和时间推进。
- 持久化 schema 升级至 v3，迁移 v2 未命名空间事件键，并兼容旧键去重，避免升级后重复应用。
- 管理页 boundary 统一使用白名单、高好感、信任与熟悉度配置门槛。
- 新增异常配置、时间回退、v2 账本迁移与去重兼容回归测试。

## 0.3.2 - 2026-07-27

### 发布阻断修复

- 统一配置默认值、类型解析与运行时范围约束，拒绝 NaN、负额度和越界阈值。
- 所有正向好感语义事件统一受非白名单上限、高好感阈值及信任/熟悉度门槛约束。
- 损坏、非法版本及未来 schema 数据安全降级为空状态，避免错误解释或覆盖未知格式。
- 命令事件改为完全只读，不写审计账本、不触发持久化与关系时间推进。
- 平台事件 ID 与去重键加入 bot/用户/会话命名空间，并限制非法或过远未来时间戳。
- 管理页高好感分段改为使用配置阈值，并补充对应回归测试。

## 0.3.1 - 2026-07-27

### 修复与调整

- 管理页 UI 对齐凝心溯溪-核更新管理器，统一深色青绿色控制台、卡片、标签和响应式布局。
- 显式加载 AstrBot Plugin Page Bridge SDK，并等待通信桥与 `ready()` 完成后再读取关系状态。
- 修复页面启动过早导致 `Cannot read properties of undefined (reading 'apiGet')` 的问题。
- 增强 API 响应解析、启动错误提示与刷新按钮请求互斥。

## 0.3.0 - 2026-07-26

### 新增与调整

- 新增参考凝心溯溪-知风格的 Plugin Page，展示关系分布、好感、信任、熟悉度、白名单和边界状态。
- 新增高好感白名单、普通朋友区上限、信任门槛与熟悉度门槛配置。
- 普通消息达到高好感区后不再继续刷分；非白名单默认不能越过朋友区。
- 页面保持只读，不提供直接改分或绕过事件审计的入口。
- 新增关系稀缺性与防刷分测试。

## 0.2.0 - 2026-07-26

### 新增与修复

- 新增不保存消息原文的事件账本、事件 ID/去重键幂等和来源证据校验。
- 将短期情绪拆为会话疲劳与会话内用户压力两层，并保持长期关系按用户隔离。
- 将信任拆分为可靠性、善意、诚信、认知四个维度，仅接受可信事实事件。
- 将每日好感正向与负向额度独立计量，避免相互吞占额度。
- 输出结构化只读行为建议；入口不再修改 LLM 请求或执行静默建议。
- 持久化 schema 升至 v2，并补齐配置、迁移与边界测试。

## 0.1.0 - 2026-07-26

### 新增

- 建立独立关系状态域与 `RelationshipStateManager`，对外提供 `record / get_snapshot / reset`。
- 迁移 `astrbot_plugin_conversation_flow` v0.6.0 的情绪追踪行为。
- 新增长期好感、最小信任、单调熟悉度和惰性时间衰减计算器。
- 新增用户长期关系与当前会话状态双作用域模型。
- 新增 JSON 持久化、`schema_version` 和版本 0 数据迁移。
- 新增关系状态到表达风格与中文提示词片段的安全映射。
- 新增 `/rel status` 与 `/rel reset` 命令。
- 新增全量可配置阈值、权重、上限、衰减速率和持久化节流参数。
- 新增离线核心测试，覆盖情绪回归、每日限幅、衰减、隔离与持久化迁移。
