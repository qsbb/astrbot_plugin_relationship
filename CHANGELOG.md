# 更新日志

## 0.3.0 - 2026-07-26

### 新增与调整

- 新增参考凝心溯溪-知风格的 Plugin Page，展示关系分布、好感、信任、熟悉度、白名单和边界状态。
- 新增高好感白名单、普通朋友区上限、信任门槛与熟悉度门槛配置。
- 普通消息达到高好感区后不再继续刷分；非白名单默认不能越过朋友区。
- 页面保持只读，不提供直接改分或绕过事件审计的入口。
- 新增关系稀缺性与防刷分测试。

## 0.2.0 - 2026-07-26

### 新增与修复

- 新增不保存消息原文的事件账本、事件 ID/去重键幂等和来源证据校验。
- 将短期情绪拆为会话疲劳与会话内用户压力两层，并保持长期关系按用户隔离。
- 将信任拆分为可靠性、善意、诚信、认知四个维度，仅接受可信事实事件。
- 将每日好感正向与负向额度独立计量，避免相互吞占额度。
- 输出结构化只读行为建议；入口不再修改 LLM 请求或执行静默建议。
- 持久化 schema 升至 v2，并补齐配置、迁移与边界测试。

## 0.1.0 - 2026-07-26

### 新增

- 建立独立关系状态域与 `RelationshipStateManager`，对外提供 `record / get_snapshot / reset`。
- 迁移 `astrbot_plugin_conversation_flow` v0.6.0 的情绪追踪行为。
- 新增长期好感、最小信任、单调熟悉度和惰性时间衰减计算器。
- 新增用户长期关系与当前会话状态双作用域模型。
- 新增 JSON 持久化、`schema_version` 和版本 0 数据迁移。
- 新增关系状态到表达风格与中文提示词片段的安全映射。
- 新增 `/rel status` 与 `/rel reset` 命令。
- 新增全量可配置阈值、权重、上限、衰减速率和持久化节流参数。
- 新增离线核心测试，覆盖情绪回归、每日限幅、衰减、隔离与持久化迁移。
